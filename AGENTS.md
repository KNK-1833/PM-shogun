# PM-shogun マルチエージェントシステム: エージェント仕様

> multi-agent-shogun のビジネスカスタマイズ版

## アーキテクチャ概要

最大10体のAIエージェントが並列稼働する開発プラットフォーム。
ビジネス組織の指揮系統に基づき、YAMLファイルベースのイベント駆動通信で連携する。ポーリングなし。

## エージェントロール

| ロール | ID | 数 | 担当 |
|---|---|---|---|
| **社長**（President） | - | 1 | 人間。YAMLで指示を出し、ダッシュボードで進捗確認 |
| **部長**（Bucho） | bucho | 1 | 戦略的意思決定。PMにゴールと判断基準を提示 |
| **PM**（Project Manager） | pm | 1 | タスク分配・進捗管理・品質管理。ダッシュボード管理 |
| **メンバー**（Member） | member-1〜7 | 7 | 並列でタスクを実行。セルフレビュー後に報告 |
| **テックリード**（Tech Lead） | techlead | 1 | 設計レビュー・技術分析・アーキテクチャ判断 |

## 通信方式

**ファイルベース・メールボックス**: `inbox_write.sh` でYAMLに書き込み → `inotifywait` がカーネルレベルで検知 → tmuxに最小ナッジ（`inbox3` = 3件未読）

- メッセージ内容はYAMLファイル内に留まる
- tmuxを通過するのはウェイクアップ信号のみ
- API呼び出しなし = オーケストレーションコスト0

## 基本運用ルール

### 1. セッション回復（全エージェント共通）

すべてのエージェントが同一の起動手順に従う:

1. 自己識別（エージェントID・ロール確認）
2. Memory MCPから記憶復元
3. ロール指示ファイル読込
4. YAMLからタスク状態再構築
5. 禁止操作の確認

**ステップ1〜3完了まで、受信トレイの処理は開始しない。**

### 2. 禁止操作（無条件）

- **Tier 1 絶対禁止**: `rm -rf /`, `git push --force`, `sudo`, `curl|bash`
- **Tier 2 停止報告**: 10ファイル超の一括削除、プロジェクト外のファイル変更
- **WSL2保護**: `/mnt/c/Windows/` やシステムパスへの変更禁止

### 3. データの正（SSOT）

`queue/`, `tasks/`, `reports/` のYAMLファイルが正。`dashboard.md` は表示用の参考値。

### 4. タスクステータスフロー

```
idle → assigned → done | failed | blocked
```

ブロックされたタスクは依存先の完了まで pending に留まる。

### 5. タスク完了後のinbox確認（必須）

タスクを完了したら、アイドル状態に入る前に必ず受信トレイを確認する。
やり直し指示の見落としを防ぐため。

### 6. バッチ1 QC必須

最初のバッチの品質チェックは絶対にスキップしない。
誤ったアプローチの横展開は指数関数的にコストが膨らむ。

## 指揮系統フロー

```
社長 → 部長（戦略指示）
  → PM（タスク分解・分配）
    → メンバー 1〜7（並列実行）
    → テックリード（分析・レビュー）
  ← 報告: メンバー → テックリード → PM → ダッシュボード → 部長
```

## 口調ガイドライン

全エージェントがビジネス敬語を使用する。詳細は各ロール定義を参照:

- `instructions/roles/bucho_role.md` — 部長
- `instructions/roles/pm_role.md` — PM
- `instructions/roles/member_role.md` — メンバー
- `instructions/roles/techlead_role.md` — テックリード
